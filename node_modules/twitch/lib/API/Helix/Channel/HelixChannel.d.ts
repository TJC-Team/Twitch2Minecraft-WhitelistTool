import type { HelixGame } from '../Game/HelixGame';
import type { ApiClient } from '../../../ApiClient';
/** @private */
export interface HelixChannelData {
    broadcaster_id: string;
    broadcaster_name: string;
    broadcaster_language: string;
    game_id: string;
    game_name: string;
    title: string;
}
/**
 * A Twitch channel.
 */
export declare class HelixChannel {
    protected _data: HelixChannelData;
    /** @private */
    protected readonly _client: ApiClient;
    /** @private */
    constructor(/** @private */ _data: HelixChannelData, client: ApiClient);
    /**
     * The ID of the channel.
     */
    get id(): string;
    /**
     * The display name of the channel.
     */
    get displayName(): string;
    /**
     * The language of the channel.
     */
    get language(): string;
    /**
     * The ID of the game currently played on the channel.
     */
    get gameId(): string;
    /**
     * The name of the game currently played on the channel.
     */
    get gameName(): string;
    /**
     * Retrieves information about the game that is being played on the stream.
     */
    getGame(): Promise<HelixGame | null>;
    /**
     * The title of the channel.
     */
    get title(): string;
}
