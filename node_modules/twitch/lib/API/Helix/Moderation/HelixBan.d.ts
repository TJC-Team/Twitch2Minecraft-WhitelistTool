import type { HelixUser } from '../User/HelixUser';
import type { ApiClient } from '../../../ApiClient';
/** @private */
export interface HelixBanData {
    user_id: string;
    user_name: string;
    expires_at: string;
}
/**
 * Information about the ban of a user.
 */
export declare class HelixBan {
    private readonly _data;
    /** @private */
    protected readonly _client: ApiClient;
    /** @private */
    constructor(_data: HelixBanData, client: ApiClient);
    /**
     * The ID of the user.
     */
    get userId(): string;
    /**
     * Retrieves more data about the user.
     */
    getUser(): Promise<HelixUser | null>;
    /**
     * The name of the user.
     */
    get userName(): string;
    /**
     * The date when the ban will expire; null for permanent bans.
     */
    get expiryDate(): Date | null;
}
