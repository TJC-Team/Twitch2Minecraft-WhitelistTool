import { Message, MessageParam, MessagePrefix } from '../../Message';
export declare type ModeAction = 'getList' | 'add' | 'remove';
export interface SingleMode {
    prefix?: MessagePrefix;
    action: ModeAction;
    letter: string;
    param?: string;
    known: boolean;
}
export declare class Mode extends Message<Mode> {
    target: MessageParam;
    modes: MessageParam;
    get isChannel(): boolean;
    separate(): SingleMode[];
}
